/**
 * Prose templates for Morph generation operations.
 *
 * This file is a FIXTURE - hand-written, survives regeneration.
 * Provides human-readable templates for test output and feature files.
 */

export const prose: Record<string, string> = {
	generate: '{actor} generates packages from schema "{name}"',
	init: '{actor} initializes a new morph project "{name}"',
	newProject: '{actor} creates a new project "{name}" from schema',
	validate: "{actor} validates schema",
};
