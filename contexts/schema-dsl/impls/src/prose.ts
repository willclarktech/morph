export const prose: Record<string, string> = {
	parseMorph: "{actor} parses .morph DSL source",
	decompileSchema: "{actor} decompiles schema to .morph DSL",
	formatDsl: "{actor} formats .morph DSL source",
	validateDsl: "{actor} validates .morph DSL source",
	getDiagnostics: "{actor} gets diagnostics for .morph source",
	getSymbols: "{actor} gets document symbols for .morph source",
	getCompletions: "{actor} gets completions at position in .morph source",
	getHover: "{actor} gets hover info at position in .morph source",
	getDefinition: "{actor} gets definition location in .morph source",
	getFoldingRanges: "{actor} gets folding ranges for .morph source",
};
