/**
 * Prose templates for code-generator operations.
 *
 * Provides human-readable templates for test output.
 */

export const prose: Record<string, string> = {
	generate: '{actor} generates code from schema with dryRun={dryRun}',
	validate: "{actor} validates schema",
};
