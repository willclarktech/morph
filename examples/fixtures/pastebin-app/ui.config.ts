import type { UiConfig } from "@morph/runtime-ui";

const config: UiConfig = {
	theme: {
		background: "#0a0a0a",
		backgroundAlt: "#141414",
		borderColor: "#27272a",
		borderRadius: "0.5rem",
		buttonShadow: "0 1px 3px rgba(0, 0, 0, 0.4)",
		cardShadow:
			"0 2px 8px rgba(34, 211, 238, 0.06), 0 1px 3px rgba(0, 0, 0, 0.3)",
		colorScheme: "dark",
		fontFamily: "'JetBrains Mono', 'Fira Code', monospace",
		headingWeight: "600",
		lineHeight: "1.6",
		primary: "#22d3ee",
		primaryFocus: "rgba(34, 211, 238, 0.2)",
		primaryHover: "#06b6d4",
		secondary: "#a78bfa",
		spacing: "2rem",
		text: "#fafafa",
		textMuted: "#a1a1aa",
		transition: "0.12s ease-out",
	},
};

export default config;
