/**
 * Prose templates for Auth-Session operations.
 *
 * This file is a FIXTURE - hand-written, survives regeneration.
 * Provides human-readable templates for test output and feature files.
 */

export const prose: Record<string, string> = {
	createSession: '{actor} creates a session for user "{userId}"',
	destroySession: '{actor} destroys session "{sessionId}"',
	validateSession: '{actor} validates session "{sessionId}"',
};
