// Examples browser — minimal client-side interactivity
// Placeholder for future enhancements (search, filtering).
// Currently examples are statically linked to the playground via URL hash.

// eslint-disable-next-line @typescript-eslint/no-empty-function -- placeholder entry point
document.addEventListener("DOMContentLoaded", () => {});
